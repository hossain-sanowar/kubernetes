kubectl get namespace
kubectl get ns
kubectl get ns --no-headers | wc -l
kubectl get pods --namespace=research
kubectl get pods --all-namespaces
kubectl get pods --all-namespaces | grep blue

kubectl logs namespace

Run the command kubectl get pods --all-namespaces
Run the command kubectl get pods --all-namespaces | grep blue

kubectl -n research get pods --no-headers | wc -l

Run the command kubectl get pods --namespace=research
Run the command to get exact the number of pods in the research namespace

Run the command kubectl get pods --all-namespaces | grep blue

Install minikube:
Minikube status
