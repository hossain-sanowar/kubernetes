# documentation install kubeadm
# install container runtime; copy and paste command control and node

cat <<EOF | sudo tee /etc/sysctl.d/k8s.conf
net.ipv4.ip_forward=1
EOF

#Apply sysctl params without reboot

sudo sysctl --system

#verify that net.ipv4.ip_forward is set to 1 with:
sysctl net.ipv4.ip_forward


ssh node01
cat /etc/*-release 
sudo apt-get update
mkdir etc/apt/keyrings


# control plane
cat /etc/*-release # see the windows
sudo apt-get update
sudo apt-get install
mkdir etc/apt/keyrings
then install one by one

sudo apt install kubeadm=26.0-00


kubelet --version
kubectl get nodes


ip add
kubeadm init --apiserver-advertise-address=192.7.229.6 --pod-network-cidr=10.244.0.0/16

then create cluster
 
kubectl get nodes


# then join the node uisng the join token

# then add network policy like flannel

kubectl get pod -A